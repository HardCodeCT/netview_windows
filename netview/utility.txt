<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Network Monitor - Minimal Gradient Design</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Inter', sans-serif;
            background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
            padding: 60px 20px;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .widget-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
        }

        .widget {
            width: 340px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 24px;
            padding: 2px;
            box-shadow: 0 10px 40px rgba(102, 126, 234, 0.4);
            transition: all 0.4s ease;
        }

        .widget:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: 0 20px 60px rgba(102, 126, 234, 0.6);
        }

        .widget-inner {
            background: #1a1a2e;
            border-radius: 22px;
            padding: 24px;
        }

        .header {
            text-align: center;
            margin-bottom: 20px;
            padding-bottom: 16px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .title {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-size: 18px;
            font-weight: 700;
            letter-spacing: 1px;
            margin-bottom: 8px;
        }

        .status {
            color: #4CAF50;
            font-size: 11px;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
        }

        .status-dot {
            width: 8px;
            height: 8px;
            background: #4CAF50;
            border-radius: 50%;
            box-shadow: 0 0 10px #4CAF50;
            animation: pulse-glow 2s infinite;
        }

        @keyframes pulse-glow {
            0%, 100% {
                opacity: 1;
                transform: scale(1);
                box-shadow: 0 0 10px #4CAF50;
            }
            50% {
                opacity: 0.7;
                transform: scale(1.2);
                box-shadow: 0 0 20px #4CAF50;
            }
        }

        .stats-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
            margin-bottom: 20px;
        }

        .stat-card {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 16px;
            padding: 20px;
            text-align: center;
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.08);
        }

        .stat-card:hover {
            background: rgba(255, 255, 255, 0.08);
            transform: translateY(-4px);
        }

        .icon-wrapper {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            margin: 0 auto 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            color: white;
        }

        .stat-card.download .icon-wrapper {
            background: linear-gradient(135deg, #00B4D8 0%, #0077B6 100%);
            box-shadow: 0 8px 16px rgba(0, 180, 216, 0.3);
        }

        .stat-card.upload .icon-wrapper {
            background: linear-gradient(135deg, #FF6B35 0%, #F77F00 100%);
            box-shadow: 0 8px 16px rgba(255, 107, 53, 0.3);
        }

        .label {
            color: rgba(255, 255, 255, 0.6);
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
        }

        .value {
            color: #ffffff;
            font-size: 22px;
            font-weight: 700;
            margin-bottom: 6px;
        }

        .rate {
            color: rgba(255, 255, 255, 0.7);
            font-size: 12px;
            font-weight: 600;
        }

        .footer {
            text-align: center;
        }

        .active-button {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 12px 28px;
            border-radius: 25px;
            font-size: 12px;
            font-weight: 700;
            letter-spacing: 0.5px;
            border: none;
            cursor: pointer;
            box-shadow: 0 4px 20px rgba(102, 126, 234, 0.4);
            transition: all 0.3s ease;
            text-transform: uppercase;
        }

        .active-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 30px rgba(102, 126, 234, 0.6);
        }

        .active-button:active {
            transform: translateY(0);
        }

        .description {
            color: rgba(255, 255, 255, 0.5);
            font-size: 14px;
            text-align: center;
            max-width: 340px;
            line-height: 1.6;
        }
    </style>
</head>
<body>
    <div class="widget-container">
        <div class="widget">
            <div class="widget-inner">
                <div class="header">
                    <div class="title">NETWORK MONITOR</div>
                    <div class="status">
                        <div class="status-dot"></div>
                        <span>ACTIVE</span>
                    </div>
                </div>

                <div class="stats-grid">
                    <div class="stat-card download">
                        <div class="icon-wrapper">↓</div>
                        <div class="label">Download</div>
                        <div class="value">2.4 GB</div>
                        <div class="rate">125 KB/s</div>
                    </div>

                    <div class="stat-card upload">
                        <div class="icon-wrapper">↑</div>
                        <div class="label">Upload</div>
                        <div class="value">856 MB</div>
                        <div class="rate">42 KB/s</div>
                    </div>
                </div>

                <div class="footer">
                    <button class="active-button">12 Processes</button>
                </div>
            </div>
        </div>

        <p class="description">
            Minimal Gradient Design • Purple gradient border with dark interior • Circular icon badges • Smooth hover animations
        </p>
    </div>
</body>
</html>